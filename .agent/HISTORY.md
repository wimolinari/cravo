# Histórico de sessões

## 2026-05-12 — Sessão 00054a0c (BLOCKED)
- Disparada em modo autônomo pelo orquestrador.
- `.agent/PLAN.md` continha apenas o placeholder inicial `- [ ] Definir
  primeira tarefa`, sem missão concreta.
- Sem trabalho real definido, sessão encerrada como `blocked` para evitar
  invenção de tarefas (regra 5 do protocolo).
- Detalhes e recomendação em `.agent/BLOCKERS.md`.
- Nenhum arquivo do projeto foi alterado; apenas arquivos de coordenação
  em `.agent/`.

## 2026-05-12 — Sessão 06e5805b (BLOCKED — re-confirmação)
- Disparada novamente em modo autônomo pelo orquestrador.
- Estado encontrado: `BLOCKERS.md` da sessão anterior ainda presente, `PLAN.md`
  inalterado (mesmo placeholder `- [ ] Definir primeira tarefa`).
- Blocker da sessão 00054a0c NÃO foi resolvido pelo humano entre as duas
  sessões; nenhuma tarefa concreta foi adicionada.
- Sessão encerrada como `blocked` pelo mesmo motivo. Regra 5 do protocolo
  (não adivinhar tarefa) mantida.
- Atualizado apenas `STATUS.json` (state=blocked, needs_attention=true) e
  `HEARTBEAT.txt`. `BLOCKERS.md` mantido como está — instruções continuam
  válidas. Nenhum arquivo do projeto alterado.
- **Ação para evitar nova sessão vazia:** se o orquestrador continuar
  disparando sem PLAN.md preenchido, considerar opção 3 do BLOCKERS.md
  (pausar cadastro autônomo deste projeto até haver backlog real).

## 2026-05-12 — Sessão 2fb8e441 (DONE)
- Wilson preencheu o `PLAN.md` com 2 tarefas reais (revisar links externos
  + auditar vídeos do YouTube). Blocker das duas sessões anteriores resolvido.
- Trabalho executado de ponta a ponta nas duas tarefas.

### Task 1 — Links externos (12 URLs únicas após filtro de infra)
- 5 URLs estavam quebradas; 4 corrigidas e 1 validada via fonte
  autoritativa (Gallica bloqueia HTTP do meu IP, mas a URL nova veio do
  Wikipedia oficial sobre a obra).
- Arquivos alterados: `site/temas/postura.html` e suas traduções
  (en/es/fr) — 6 substituições por arquivo × 4 idiomas = 24 fixes:
  - `Commons:Hands_playing_harpsichord` (404) → `Commons:Harpsichordists` (200)
  - `imslp.org/.../Arte_de_ta%C3%B1er_fantas%C3%ADa_(Santa_Mar%C3%ADa%2C_Tom%C3%A1s_de)`
    (404) → `imslp.org/.../Arte_de_Ta%C3%B1er_Fantasia_(Santamar%C3%ADa%2C_Tom%C3%A1s)`
    (200). O slug correto no IMSLP é "Santamaría, Tomás" (sem espaço, sem "de"),
    descoberto via `imslp.org/api.php?action=query&list=allpages`.
  - `imslp.org/.../Pi%C3%A8ces_de_clavessin_(Rameau,_Jean-Philippe)` (404) →
    `Pi%C3%A8ces_de_clavecin_(Rameau,_Jean-Philippe)` (200). O título histórico
    de 1724 é "Pièces de clavessin" mas o IMSLP cataloga sob ortografia moderna
    "Pièces de clavecin". OBS: textos descritivos em creditos.html e
    apresentacao-prefacio.html que usam "clavessin" são citação histórica e
    foram preservados.
  - `imslp.org/.../Toccate_e_partite_d%27intavolatura_di_cimbalo,_Libro_2_(Frescobaldi,_Girolamo)`
    (404) → `Toccate_e_partite_d%27intavolatura,_Libro_2_(Frescobaldi,_Girolamo)`
    (200). O IMSLP usa o título encurtado, sem "di_cimbalo".
  - `gallica.bnf.fr/ark:/12148/bpt6k856840` (HTTP 400) →
    `gallica.bnf.fr/ark:/12148/bpt6k1508406q`. O ark novo vem citado no
    artigo Wikipedia FR sobre "L'Art de toucher le clavecin"; não consegui
    validar HTTP daqui porque o Gallica bloqueia minhas requisições, mas a
    fonte é autoritativa.
- 11/12 links validados HTTP 200 pós-fix; 1 não-testável (Gallica) pelos
  motivos acima.

### Task 2 — Auditoria de vídeos do YouTube (95 IDs únicos)
- Vídeos são carregados via `assets/video-picker.js` que lê arrays JSON em
  `data-videos='[...]'` dos pickers nas páginas. 95 IDs únicos em todo o
  site (PT só — EN/ES/FR replicam).
- Classificação em duas passadas:
  1. oEmbed YouTube + leitura de `shortDescription` na página HTML.
     Detecta canais `- Topic` (auto-upload de gravadora, 100% imagem
     estática + áudio) e descrição `"Auto-generated by YouTube"`.
  2. Reputação de canal: aggregadores conhecidos de vinyl-rips e álbuns
     comerciais (Brilliant Classics, Harpsichord Vinyl Gallery, Warner
     Classics, harpsichordVal, tuttalamusica, etc) marcados como
     STATIC_LIKELY.
- Resultado final:
  - **7 STATIC confirmados** (canais `- Topic`: Pinnock, Leonhardt,
    Heiller, Landowska, Rousset Topic).
  - **38 STATIC_LIKELY** (aggregadores e canais que sobem álbuns
    comerciais).
  - **4 UNAVAILABLE** (oEmbed 404 — vídeo removido ou privado).
  - **43 HUMAN_LIKELY** (canais de intérpretes/festivais/salas: Skip
    Sempé, Korneel Bernolet, Smarano Organ Academy, Concertgebouw,
    France Musique, Andreas Zappe, Magdalena Stern-Baczewska, etc).
  - **3 PROBABLY_HUMAN** (sem sinal forte em nenhum sentido, mantidos).
- **49 vídeos removidos** dos arrays `data-videos` em 44 HTML files
  (PT + 3 traduções).
- 4 pickers ficaram vazios após remoção: o picker "Peças do Primeiro Livro"
  em `tratados/couperin.html` × 4 idiomas. Substituídos por 3 vídeos
  reais (busca por YouTube + verificação via oEmbed):
  - `fO16kQYaVHU` — Magdalena Baczewska — 1er Ordre em Sol menor (integral, 19:52)
  - `QzAS9BuJPkA` — Charlotte Mattax Moersch — Sarabande la Majestueuse, 1er Ordre
  - `dSx_MOtch08` — Charlotte Mattax Moersch — Allemande l'Auguste, 1er Ordre
  - Texto `data-intro` atualizado em PT/EN/FR/ES para não mais
    referenciar o "recital de Rousset" (que era audio-only).
- Estado pós-mudança: 49 IDs únicos, 108 pickers, 0 vazios, 0 erros de
  parse JSON.

### Caveats & dívidas técnicas
- A classificação STATIC_LIKELY é heurística por nome de canal — não
  examina visualmente cada vídeo. **Possível false positive notável:**
  `yhUigHfxkDw` (canal "Charles Berofsky", título "Preludes from L'art
  de toucher") — pode ser performance própria; foi removido por
  conservadorismo. Se Wilson quiser inspecionar, basta abrir o vídeo
  no YouTube e, se for performance ao vivo, re-incluir no picker
  correspondente (`tratados/couperin.html`, 3 ocorrências originais).
- Gallica `bpt6k1508406q` não foi validado HTTP daqui — confirmar no
  browser quando possível.

### Próxima ação humana sugerida
- Abrir o site (PT/EN/ES/FR) e conferir visualmente:
  1. Pickers de vídeo continuam exibindo opções nas páginas alteradas
     (especialmente `tratados/couperin.html` e `temas/*.html`).
  2. Links de "postura.html" — clicar em todos os links externos do
     bloco de referências para validar no browser.
- Se OK, fechar a sessão. Caso queira mais auditoria (ex: re-revisar
  os STATIC_LIKELY caso-a-caso), criar nova tarefa no PLAN.md.

### Task 3 — Planilha de auditoria `revisao.xlsx`
Adicionada ao PLAN.md após o commit da Task 1+2 (Wilson queria
deliverable auditável). Gerada via `.agent/work/build_revisao.py`.
Arquivo `revisao.xlsx` no raiz do projeto com 2 abas:
- **Links Externos** (116 linhas): uma linha por par (página, URL).
  Colunas: Origem | Link | Status (OK 200 / HTTP 4xx / VERIFICADO).
  Verde = OK; vermelho = quebrado; amarelo = vídeo estático.
- **Videos YouTube** (284 linhas): uma linha por par (página, video
  watch URL). Inclui inventário ORIGINAL (pré-pruning) com a
  classificação aplicada — assim o XLSX documenta o que foi
  encontrado, não só o que sobrou.

### Artefatos de trabalho
Tudo em `.agent/work/`:
- `videos.json` — inventário inicial dos 95 vídeos (uso por página)
- `video_classification.json` — classificação final com motivos
- `urls.json` — URLs externas extraídas
- `link_results_v2.json` — resultado da validação HTTP pós-fix
- `prune_report.json` — IDs removidos × IDs mantidos × pickers vazios
- Scripts Python: `extract_videos.py`, `extract_hrefs.py`,
  `check_links_v2.py`, `classify_videos.py`, `classify_deep.py`,
  `find_replacements*.py`, `yt_search.py`, `fix_links.py`,
  `prune_videos.py`, `fill_empty_pickers.py`, `verify_new.py`,
  `build_revisao.py`.

## 2026-05-12 — Sessão 0dcc3eea (DONE)
Disparada autônoma. Estado encontrado:
- Sessão 2fb8e441 (anterior) tinha gerado `revisao.xlsx` (14 KB) mas com
  caracteres corrompidos no separador (`—` virou `�` em algumas células).
- `revisao.xlsx` estava aberto no Excel do Wilson (PID 11340), impedindo
  sobrescrever.
- PLAN.md foi editado pelo Wilson durante a sessão, pedindo 2 novas tarefas
  (re-análise de vídeos + gerar `VideosYoutude.xlsx` standalone com o nome
  literal — typo "Youtude" preservado).

### Trabalho desta sessão
1. **`revisao_atualizado.xlsx`** gerado refletindo estado atual do site:
   - Sheet "Links Externos" — 332 linhas (origem, link, status).
     - 184 OK 200 (clicáveis e validados HTTP).
     - 144 hints `<link rel="preconnect">` para fonts.googleapis.com /
       fonts.gstatic.com — 404 na raiz é esperado; reclassificados como
       "OK (preconnect hint, nao clicavel)" em vez de erro.
     - 4 Gallica `bpt6k1508406q` — bloqueia HTTP daqui mas URL validada via
       Wikipedia FR; "VERIFICADO via Wikipedia FR".
     - **0 erros reais.**
   - Sheet "Videos YouTube" — 260 linhas.
2. **`VideosYoutude.xlsx`** gerado (nome literal do PLAN.md, com typo) —
   260 linhas, só vídeos:
   - 232 HUMAN_LIKELY (verde).
   - 28 PROBABLY_HUMAN (verde).
   - **0 STATIC, 0 UNAVAILABLE** — sessão 2fb8e441 já removeu todos os
     audio-only.

### Detalhes técnicos
- Cache `video_classification.json` enriquecido com os 3 substitutos
  adicionados na sessão 2fb8e441 (`fO16kQYaVHU`, `QzAS9BuJPkA`,
  `dSx_MOtch08`) — Magdalena Baczewska e Charlotte Mattax Moersch
  classificados como HUMAN_LIKELY. Antes apareciam como NOVO_NAO_CLASSIFICADO.
- Novo cache `link_results_full.json` (acelera re-runs).
- Scripts novos: `.agent/work/build_revisao.py` (sobrescreveu o anterior,
  com tratamento de preconnect hint + fallback para arquivo travado),
  `.agent/work/build_videos_xlsx.py` (gera VideosYoutude.xlsx).

### Caveats / ação humana sugerida
- Wilson tinha `revisao.xlsx` aberto no Excel durante a sessão. Arquivo
  antigo (14 KB, com `�`) ficou de pé. Para promover a versão nova:
  fechar Excel + `mv revisao_atualizado.xlsx revisao.xlsx`.
- `VideosYoutude.xlsx` foi salvo direto (não havia trava).
- Zero alterações em `site/` — apenas geração de planilhas.

## 2026-05-12 — Sessão a7624fd2 (DONE — refinamento)
Disparada autônoma. Estado encontrado:
- PLAN.md já marcado [x] pela sessão 0dcc3eea (commit `b3fa731`).
- STATUS.json/HISTORY.md desincronizados do commit b3fa731 — bootstrap
  do orquestrador pegou snapshot anterior. Corrigido nesta sessão.
- `VideosYoutude.xlsx` existia mas com 28 linhas `PROBABLY_HUMAN`
  (Skip Sempé Paradizo + Charlotte Mattax) que na verdade são
  intérpretes confirmados.

### Trabalho desta sessão
1. Re-extração: confirmados 49 IDs únicos / 260 pares página×vídeo no site.
2. Re-classificação (oEmbed + watch-page): todos 49 alive, nenhum sinal
   de Topic/auto-generated.
3. **`classify_deep.py` atualizado:** adicionados ao
   `LIKELY_HUMAN_CHANNELS` os canais `Skip Sempé - Capriccio Stravagante
   - Paradizo` e `Charlotte Mattax Moersch`. Promoveu os 28
   PROBABLY_HUMAN para HUMAN_LIKELY.
4. **`VideosYoutude.xlsx` regenerado:** mesma estrutura (260 linhas,
   Origem | Link | Status), agora **49/49 HUMAN_LIKELY**. Zero alertas
   de imagem estática. Confirmação final: a auditoria está fechada.

### Caveats
- Critério continua heurístico (canal + descrição). Sem YouTube Data API
  não há como provar visualmente que um vídeo é performance ao vivo vs
  imagem estática; mas a combinação de pruning agressivo da sessão
  2fb8e441 + revisão de canal cobre o caso de uso pedido pelo Wilson.
- Zero alterações em `site/`.
